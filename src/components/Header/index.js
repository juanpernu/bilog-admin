import React from 'react';
import './index.scss';
import NavBar from '../Navbar';

const Header = () => {
    return (
        <div className="header-container">
            <NavBar />
        </div>
    );
};

export default Header;
